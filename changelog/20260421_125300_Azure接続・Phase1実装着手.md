# Azure接続・Phase1実装着手

## 作業日
2026-04-21

## 作業内容

### Azure PostgreSQL接続確立
- `prisma/schema.prisma` のproviderを `sqlite` → `postgresql` に変更
- `prisma.config.ts` にDATABASE_URLを設定する方式に対応（Prisma 7仕様）
- `.env` にAzure PostgreSQL接続文字列を設定（パスワード特殊文字はURLエンコード済み）
- `npx prisma migrate dev --name init` でAzureに19テーブル作成完了
- VSCode PostgreSQL拡張機能（Microsoft製）でDB接続確認済み

### 設計方針確定
- デプロイ先：Vercel（ローカルdev serverは使用しない）
- DBはAzure PostgreSQL一択（SQLiteは採用しない）
- CSVインポートは暫定対応（将来AI汎用取込設計に移行）
- プロジェクトを2軸に分離：
  - **customer-dm-app**（これ）：早期に営業メールを送ることに特化
  - **別リポジトリ（将来）**：CRM基盤DB・AI汎用データ取込・ナレッジDB

### 設計書作成・更新
- `docs/phase1-implementation-plan.md` 新規作成
- `README.md` にプロジェクト全体構成を追記

### Phase 1実装（着手）
- `app/layout.tsx`：サイドバー付きレイアウトに更新
- `app/page.tsx`：ダッシュボード（統計・メニュー）作成
- `tsconfig.json`：パスエイリアス `@/*` を `./src/*` に修正
- `papaparse` / `@types/papaparse` インストール
- `src/lib/importers/engine.ts` 新規作成（インポートエンジン）
- `app/api/import/route.ts` 新規作成（インポートAPI）
- `app/import/page.tsx` 新規作成（CSVインポート画面）

## 未完了・次回以降

- `import_sources` テーブルへのシードデータ投入（MUSUBU・Urizo）
- Step 3：企業一覧・フィルタリング画面
- Step 4：企業詳細画面
- Step 5：送信対象リスト管理
- Vercelデプロイ・動作確認
